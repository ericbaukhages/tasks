{
  "name": "tasks-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "test": "tsc -b tsconfig.test.json && node --test 'dist-test/**/*.test.js'",
    "serve": "node dist/http/server.js",
    "mcp": "node dist/mcp/server.js"
  },
  "engines": {
    "node": ">=22.13.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.6.0",
    "@tasks/types": "*",
    "fastify": "^5.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.3"
  }
}
